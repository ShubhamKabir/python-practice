\# Program 08: Config Reader

A utility to load application settings from an external configuration file.



\### Why this matters:

Allows users to change app settings without editing the Python source code.

