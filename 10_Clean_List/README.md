# Program 10: Line Cleaner

A utility to clean up text files by removing whitespace and skipping empty lines.

### Logic:
- Uses .strip() to clean each line.
- Uses an 'if' condition to filter out blank lines.
